#include "C:\Users\wolka\OneDrive\Документы\GitHub\pugixml\src/pugixml.cpp" 
#include <iostream>
#include <vector>
#include <map>
using namespace std;
    
class CTransport
{
private:
    map <string, int> _M_Tr;
    string _route_Of_Tr;
public:
    CTransport() {};
    CTransport(string route) : _route_Of_Tr(route) {}

    void Get_rout()
    {

    }


    void routes()
    {
        pugi::xml_document document;
        if (!(document.load_file("dataset.xml")))
            throw exception("Error while opening 'dataset.xml'");


    }

};




















int main()
{
    CTransport example("IMMA_DUDE");
    example.routes();

    return 0;
}

